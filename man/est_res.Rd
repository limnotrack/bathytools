% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{est_res}
\alias{est_res}
\title{Estimate an appropriate raster resolution for a spatial polygon}
\usage{
est_res(
  x,
  target_cells = 500,
  min_res = NULL,
  max_res = NULL,
  snap_to = c(2, 4, 8, 16, 20, 24, 32, 40, 64)
)
}
\arguments{
\item{x}{An sf polygon or multipolygon object}

\item{target_cells}{Target number of cells across the shorter dimension (default 500)}

\item{min_res}{Minimum resolution in CRS units (default NULL = no floor)}

\item{max_res}{Maximum resolution in CRS units (default NULL = no ceiling)}

\item{snap_to}{Optional numeric vector of "nice" values to snap to (e.g. c(5, 10, 25, 50, 100))}
}
\value{
A single numeric value: the estimated resolution in CRS units
}
\description{
Scales resolution based on polygon area using a power-law relationship,
so small areas get fine resolution and large areas get coarser resolution.
}
\examples{
est_res(nz_coast)                        # auto-scale
est_res(nz_coast, target_cells = 200)    # coarser
est_res(nz_coast, snap_to = c(10, 25, 50, 100, 250, 500))
}
