% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_depth_at_point.R
\name{extract_depth_at_point}
\alias{extract_depth_at_point}
\title{Extract Lake Depth at a Given Point}
\usage{
extract_depth_at_point(
  x,
  bathy_raster = NULL,
  depth_points = NULL,
  contours = NULL,
  crs = NULL,
  method = NULL,
  max_dist = Inf,
  idw_power = 2,
  n_neighbors = 5
)
}
\arguments{
\item{x}{numeric or sf POINT. Either a numeric vector of length 2 containing
the x and y coordinates (c(x, y)), or an sf POINT object. If numeric
coordinates are provided, the \code{crs} parameter must also be specified.}

\item{bathy_raster}{SpatRaster object with the bathymetry data. Optional if
\code{depth_points} or \code{contours} is provided. If provided along with other
data sources, this will be used preferentially.}

\item{depth_points}{sf POINT object with depth data. Must contain a 'depth'
column. Optional if \code{bathy_raster} or \code{contours} is provided. If provided
along with \code{contours} (but not \code{bathy_raster}), this will be used preferentially.}

\item{contours}{sf LINESTRING or MULTILINESTRING object with contour data.
Must contain a 'depth' column. Optional if \code{bathy_raster} or \code{depth_points}
is provided. This has the lowest priority if multiple data sources are provided.}

\item{crs}{numeric or character. Coordinate reference system (CRS) of the
input coordinates if \code{x} is a numeric vector. Can be an EPSG code or proj4
string. Required if \code{x} is numeric; ignored if \code{x} is an sf object.}

\item{method}{character. Method to use for extraction. Options are:
\itemize{
\item "bilinear": bilinear interpolation from raster (default for raster)
\item "simple": nearest cell value from raster
\item "nearest": nearest neighbor from point data or contours (default for point/contour data)
\item "idw": inverse distance weighting from point data
}
Default is "bilinear" for raster data and "nearest" for point/contour data.}

\item{max_dist}{numeric. Maximum distance (in map units) to search for
nearest neighbor or for IDW interpolation when using point or contour data.
Default is Inf (no distance limit).}

\item{idw_power}{numeric. Power parameter for inverse distance weighting.
Default is 2.}

\item{n_neighbors}{integer. Number of nearest neighbors to use for IDW
interpolation. Default is 5.}
}
\value{
numeric. The depth value at the specified point. Returns NA if no
valid depth can be extracted (e.g., point is outside the raster extent,
or no nearby points/contours are found within max_dist).
}
\description{
This function extracts the lake depth at a specific point location from
various data sources: bathymetry raster, point depth data, or contour data.
If multiple data sources are provided, the function will use them in the
following priority order: bathymetry raster > point data > contours.
}
\examples{
\dontrun{
# Load example data
shoreline <- readRDS(system.file("extdata/rotoma_shoreline.rds",
                                 package = "bathytools"))
depth_points <- readRDS(system.file("extdata/depth_points.rds",
                                  package = "bathytools"))

# Create bathymetry raster
bathy <- rasterise_bathy(shoreline = shoreline, depth_points = depth_points,
                         crs = 2193, res = 8)

# Extract depth at a point from raster
depth <- extract_depth_at_point(x = c(2823700, 6404300),
                                bathy_raster = bathy,
                                crs = 2193)

# Extract depth from point data
depth <- extract_depth_at_point(x = c(2823700, 6404300),
                                depth_points = depth_points,
                                crs = 2193,
                                method = "nearest")

# Extract depth using IDW from point data
depth <- extract_depth_at_point(x = c(2823700, 6404300),
                                depth_points = depth_points,
                                crs = 2193,
                                method = "idw",
                                n_neighbors = 5)

# Extract depth from contours
contours <- get_contours(bathy_raster = bathy)
depth <- extract_depth_at_point(x = c(2823700, 6404300),
                                contours = contours,
                                crs = 2193,
                                method = "nearest")
}

}
