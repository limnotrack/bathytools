% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_lake_morphometry.R
\name{batch_lake_morphometry}
\alias{batch_lake_morphometry}
\title{Apply calc_lake_morphometry to a named list of SpatRasters}
\usage{
batch_lake_morphometry(
  raster_list,
  parallel = FALSE,
  n_cores = parallel::detectCores() - 1L,
  ...
)
}
\arguments{
\item{raster_list}{A named list of \code{terra::SpatRaster} objects, one per
lake. If unnamed, lakes are labelled \code{lake_1}, \code{lake_2}, etc.}

\item{parallel}{Logical. If \code{TRUE}, processing is distributed across
multiple CPU cores using \code{parallel::makeCluster()} /
\code{parallel::parLapply()}. Default \code{FALSE}.}

\item{n_cores}{Integer. Number of parallel workers to use when
\code{parallel = TRUE}. Defaults to \code{parallel::detectCores() - 1L}, leaving
one core free for the main R session. Silently capped at the number of
lakes so no idle workers are created.}

\item{...}{Additional arguments passed to \code{calc_lake_morphometry()}.}
}
\value{
A \code{data.frame} with one row per successfully processed lake and one
column per morphometric metric, plus a leading \code{lake_id} column. Lakes
that raise an error are dropped with a \code{warning()} and do not appear in
the output.
}
\description{
Iterates \code{calc_lake_morphometry()} over a named list of bathymetric rasters
and returns results as a single tidy \code{data.frame} with one row per lake.

Parallel execution is supported via the \pkg{parallel} package (base R).
When \code{parallel = TRUE} the function creates a PSOCK cluster with \code{n_cores}
workers, exports the required functions and packages to each worker, then
distributes lakes across workers using \code{parallel::parLapply()}. The cluster
is always stopped on exit — including on error — via \code{on.exit()}, so no
manual cleanup is needed.

\strong{Choosing a backend}
\itemize{
\item \code{parallel = FALSE} (default): single-threaded \code{lapply()}. Safe in all
contexts including interactive sessions and RMarkdown.
\item \code{parallel = TRUE}: spawns \code{n_cores} PSOCK workers. Recommended when
processing many lakes (roughly > 20) on a machine with multiple cores.
Note that \pkg{terra} SpatRaster objects are serialised to each worker;
very large rasters may make parallelisation slower than sequential
processing due to inter-process data transfer overhead.
}
}
\examples{
\dontrun{
  files     <- list.files("bathymetry/", pattern = "\\\\.tif$", full.names = TRUE)
  rasters   <- stats::setNames(lapply(files, terra::rast),
                               tools::file_path_sans_ext(basename(files)))

  # Sequential
  results   <- batch_lake_morphometry(rasters)

  # Parallel — use 4 cores
  results_p <- batch_lake_morphometry(rasters, parallel = TRUE, n_cores = 4)
}
}
